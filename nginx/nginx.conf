server {
    listen 443 ssl;
    server_name xadreznaron.es;
    http2 on;
    
    # Localización del certificado.
    ssl_certificate /etc/nginx/certs/xadreznaron.es/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/xadreznaron.es/privkey.pem;
    # Incluir configuración SSL/TLS común
    include /etc/nginx/conf.d/ssl_common.conf; 

    # Agregar encabezado HSTS  acceder al sitio web solo a traves de HTTPS
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
    # Solo se permite cargar la página en un marco/iframe si proviene del mismo dominio
    add_header X-Frame-Options "SAMEORIGIN" always;
    # Agregar encabezado X-Content-Type-Options
    add_header X-Content-Type-Options "nosniff" always;
    # Bloquear todo el contenido externo y permitir solo el contenido que provenga del propio host
    add_header Content-Security-Policy "script-src 'self'; frame-ancestors 'self'; style-src 'self' 'unsafe-inline' fonts.googleapis.com cdn.jsdelivr.net; object-src 'self'   " always;




    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    # Excluir archivos que no necesitan ser comprimidos
    gzip_types application/pdf image/jpeg image/png image/avif;
    gzip_proxied any;
    gzip_vary on;
    gzip_disable "MSIE [1-6]\.(?!.*SV1)";

    location / {
        # Directorio donde se almacenan los archivos estáticos de tu aplicación React
        root /usr/share/nginx/html;

        # Intenta servir el archivo solicitado; si no existe, redirige a index.html
        try_files $uri /index.html;

        # Incluir configuración común
        include /etc/nginx/conf.d/common.conf;

        # Añade formatos de imagen a la configuración de caché
        location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|avif)$ {
            expires 1y;  # Duración de caché de 1 año
            add_header Cache-Control "public, max-age=31536000";
        }
    }
}
server {
    listen 443 ssl;
    server_name www.xadreznaron.es;
    http2 on;
    
    # Localización del certificado.
    ssl_certificate /etc/nginx/certs/www.xadreznaron.es/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/www.xadreznaron.es/privkey.pem;
    # Incluir configuración SSL/TLS común
    include /etc/nginx/conf.d/ssl_common.conf; 

    # Agregar encabezado HSTS  acceder al sitio web solo a traves de HTTPS
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    # Solo se permite cargar la página en un marco/iframe si proviene del mismo dominio
    add_header X-Frame-Options "SAMEORIGIN" always;
    # Agregar encabezado X-Content-Type-Options
    add_header X-Content-Type-Options "nosniff" always;
    # Bloquear todo el contenido externo y permitir solo el contenido que provenga del propio host google fonts y ...
    add_header Content-Security-Policy "script-src 'self'; form-action 'self'; frame-ancestors 'self'; style-src 'self' 'unsafe-inline' fonts.googleapis.com cdn.jsdelivr.net; object-src 'self'   " always;
    # el navegador no envia encabezado Referer, contiene la URL de la pagina desde la cual se origino la solicitud.
    add_header Referrer-Policy "no-referrer";
    # caracteristicas desabilidatas. add_header Permissions-Policy "" -> vacio, lo controla el navegador.
    add_header Permissions-Policy "accelerometer=(), camera=(), microphone=(), geolocation=(), gyroscope=(), magnetometer=(), payment=(), usb=(), vr=()";

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    # Excluir archivos que no necesitan ser comprimidos
    gzip_types application/pdf image/jpeg image/png image/avif;
    gzip_proxied any;
    gzip_vary on;
    gzip_disable "MSIE [1-6]\.(?!.*SV1)";


    location / {
        # Directorio donde se almacenan los archivos estáticos de tu aplicación React
        root /usr/share/nginx/html;

        # Intenta servir el archivo solicitado; si no existe, redirige a index.html
        try_files $uri /index.html;

        # Incluir configuración común
        include /etc/nginx/conf.d/common.conf;

        # Añade formatos de imagen a la configuración de caché
        location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|avif)$ {
            expires 1y;  # Duración de caché de 1 año
            add_header Cache-Control "public, max-age=31536000";
        }
    }
}